import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Page = () => {
    return (
      <SafeAreaView>
        <Text>Página de PRELOAD</Text>
      </SafeAreaView>
    );
  }

export default Page;